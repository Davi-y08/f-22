import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "br.com.stealthlens.mobile",
  appName: "Stealth Lens",
  webDir: "dist",
};

export default config;
